<Window x:Class="CcCalendar.Desktop.ColorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="更多颜色"
        Width="430"
        Height="390"
        MinWidth="430"
        MinHeight="390"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="18" FontWeight="SemiBold" Text="自定义颜色" />
        <Border x:Name="ColorPreview"
                Grid.Row="2"
                Height="44"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4" />
        <Grid Grid.Row="4">
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
            <Grid.ColumnDefinitions><ColumnDefinition Width="28" /><ColumnDefinition Width="*" /><ColumnDefinition Width="76" /></Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Text="R" />
            <Slider x:Name="RedSlider" Grid.Column="1" Maximum="255" Minimum="0" ValueChanged="ChannelChanged" />
            <TextBox x:Name="RedBox" Grid.Column="2" Height="28" Margin="8,0,0,0" VerticalContentAlignment="Center" LostFocus="ChannelBoxLostFocus" />
            <TextBlock Grid.Row="1" VerticalAlignment="Center" FontWeight="SemiBold" Text="G" />
            <Slider x:Name="GreenSlider" Grid.Row="1" Grid.Column="1" Maximum="255" Minimum="0" ValueChanged="ChannelChanged" />
            <TextBox x:Name="GreenBox" Grid.Row="1" Grid.Column="2" Height="28" Margin="8,0,0,0" VerticalContentAlignment="Center" LostFocus="ChannelBoxLostFocus" />
            <TextBlock Grid.Row="2" VerticalAlignment="Center" FontWeight="SemiBold" Text="B" />
            <Slider x:Name="BlueSlider" Grid.Row="2" Grid.Column="1" Maximum="255" Minimum="0" ValueChanged="ChannelChanged" />
            <TextBox x:Name="BlueBox" Grid.Row="2" Grid.Column="2" Height="28" Margin="8,0,0,0" VerticalContentAlignment="Center" LostFocus="ChannelBoxLostFocus" />
        </Grid>
        <Grid Grid.Row="6" Margin="0,16,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" Text="RGB 0–255" />
            <Button Grid.Column="1" Width="72" Margin="8,0,0,0" Click="CancelClick" Content="取消" IsCancel="True" />
            <Button Grid.Column="2" Width="72" Margin="8,0,0,0" Click="ConfirmClick" Content="确定" IsDefault="True" Style="{StaticResource PrimaryButtonStyle}" />
        </Grid>
    </Grid>
</Window>
